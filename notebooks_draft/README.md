# Draft notebook workflow (not runnable)

This folder contains a draft notebook-based version of the workflow. The notebooks and the helper
scripts here depend on modules and data files that are not part of this repository, so they cannot be
executed as they stand, and they are kept only for reference.

The working protocol for this study is the README at the top level of the repository, which documents
the analysis scripts that reproduce the reported results.